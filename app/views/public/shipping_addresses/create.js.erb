$('.js-form')[0].reset();
$('#error').html("<%= j(render 'layouts/error_messages', model: @address_date) %>");
$('#index').html("<%= j(render "index", address_dates: @address_dates) %>");
$('#flash-message').html('<%= j(render "layouts/flash_message") %>');

function sleep(msec) {
   return new Promise(function(resolve) {

      setTimeout(function() {resolve()}, msec);

   })
};

$(document).ready(function (){
  $('#flash-message').slideDown(1000,async function start(){
    await sleep(2000);
    $('#flash-message').slideUp(1000);
  });
});